version: v2
modules:
  - path: libs/atlas-proto/protocat
lint:
  use:
    - DEFAULT
  except:
    - PACKAGE_DIRECTORY_MATCH
    - DIRECTORY_SAME_PACKAGE
    - PACKAGE_VERSION_SUFFIX
breaking:
  use:
    - FILE
